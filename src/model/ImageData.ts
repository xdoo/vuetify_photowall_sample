export default class ImageData {

    id: number
    src: string
    width: number
    height: number

    constructor(id: number ,src: string, width: number, height: number) {
        this.id = id
        this.src = src
        this.width = width
        this.height = height
    }

}